---
description: Git 提交与 push 分工（SSH 仅在 WSL）
alwaysApply: true
---

# Git 提交与 push

本机 **GitHub SSH 只在 WSL 里配置**，Windows PowerShell 直接 `git push` 会 `Permission denied (publickey)`。

## `/commit` 或手动提交

1. 先并行查看 `git status`、`git diff`、`git log -5 --oneline`，再写 1–2 句 commit message。
2. **默认后台提交**：用 Task `subagent_type: shell` + `run_in_background: true` 执行 `.\commit.ps1`（带 `-p` 则 push）；主对话立即继续，勿 Await。用户要同步等结果时用 `/commit -sync -p`。
3. 在项目根目录（前台 `-sync` 或用户手动）：
   ```powershell
   .\commit.ps1 "commit message"      # 仅 commit
   .\commit.ps1 -p "commit message"   # commit + push
   ```
4. **默认流程**（`commit.ps1`）：
   - **stage + commit**：Windows git（稳定，避免 WSL 引号/`set -e` 问题）
   - **push**（`-p`）：WSL 执行 `./commit.sh --push-only`（走 SSH）
5. 需要 **WSL 的 git 作者信息** 时（少见）：
   ```powershell
   .\commit.ps1 -Wsl -p "commit message"
   ```
   消息经临时文件传入，避免 shell 引号被拆散。
6. 或在 WSL 内全流程：
   ```bash
   cd /mnt/e/workspace/andriod-reader
   ./commit.sh -p "commit message"
   ```

## 禁止提交

`pat.txt`、`local.properties`、`.env`、`app/build/`、`.gradle/` 等（脚本已 exclude，勿 `git add -f`）。

## 约束

- 不要 `git push --force` 到 main/master
- 不要改 git config
- 无变更不空提交；只有用户带 `-p` 才 push
